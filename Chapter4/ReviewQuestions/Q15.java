/* Q15: The partitioningBy() collector creates a Map<Boolean, List<String>> when passed to collect by default. When specific parameters are pased to partitioningBy(), which return types can be created? (Choose all that apply) 
*
* D, E
* Map<Boolean, List<String>>
* Map<Boolean, Set<String>>
*/